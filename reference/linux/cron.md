# Cron

Cron is a task scheduler that can run jobs

```bash
#directories
/etc/cron
/etc/cron.daily
/etc/cron.hourly
/etc/cron.weekly
/etc/cron.monthly

#commands
 crontab -l (list) 
 crontab -e (edit) 
```

